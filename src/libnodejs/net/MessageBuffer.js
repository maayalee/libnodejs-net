/*jshint esnext: true */
/**
 * @class MessageBuffer
 *
 * @brief 
 *        
 * @author Lee, Hyeon-gi
 */
class MessageBuffer {
  constructor() {
  }

  /**
   * 데이터를 추가한다.
   * 
   * @param data Binary 데이터
   */
  push(data) {
  }

  /**
   * 하나의 메시지 단위로 완성된 데이터를 리턴한다.
   * 
   * @return array(Binary)
   */
  pullMessages() {
  }
}

module.exports = MessageBuffer;
